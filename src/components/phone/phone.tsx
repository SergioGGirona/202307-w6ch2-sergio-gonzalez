import { Actions } from '../actions/actions';
import { Keyboard } from '../keyboard/keyboard';

export function Phone() {
  return (
    <main>
      <Keyboard></Keyboard>
      <Actions></Actions>
    </main>
  );
}
